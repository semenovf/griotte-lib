#pragma once
#include <pfs/griotte/painter/qt.hpp>

namespace pfs {
namespace griotte {

using painter = qt::painter;

/**
 * @class brush
 * @brief Brush defines how shapes are filled.
 */
using brush = qt::brush;

/**
 * @class pen
 * @brief Pen defines how draw lines and outlines of shapes,
 *        and it also defines the text color.
 */
using pen = qt::pen;

}} // namespace pfs::griotte
